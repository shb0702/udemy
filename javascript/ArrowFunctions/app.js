//exe
const square = (x) => {
    return x * x;
}

const sum = (x, y) => {
    return x + y;
}

const add = function(x,y){
    return x + y;
}

//Arrow function 
const add1 = (x,y) => {
    return x + y;
}

const square1 = (x) => {
    return x * x;
}

const rollDie = () => {
    return Math.floor(Math.random() * 6) + 1
}

